# Insight Synergy Demo

This slot is now filled by `demos/insight-synergy`. Keep the generator-driven
pattern: the demo should be rebuilt via `python generate_demo.py` whenever the
runtime behaviour shifts.
